import {makeSuite} from './helpers/make-suite';

import {makeSuite, TestEnv} from './helpers/make-suite';

const {expect} = require('chai');

makeSuite('Aave Governance V2 tests', (testEnv: TestEnv) => {
  it('WIP tests', async () => {
    expect(false).to.be.true('Do tests');
  });
});
